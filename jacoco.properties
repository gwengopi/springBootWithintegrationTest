# Change the output format to XML
# Define the output directory for code coverage reports
outputDirectory = target/jacoco-reports
excludes = *HibernateProxy*
# Set the format for code coverage reports to HTML
output = html